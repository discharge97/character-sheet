import type {CapacitorConfig} from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alex_s.dndCharSheet',
  appName: 'DnD Character Sheet',
  webDir: 'dist/character-sheet/browser',
};

export default config;
